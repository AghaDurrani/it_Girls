apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "streamlit-template.fullname" . }}-test-connection"
  labels:
    {{- include "streamlit-template.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "streamlit-template.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

